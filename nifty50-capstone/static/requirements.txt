# Dependencies for the deployment package (the FastAPI service, the CLI client and
# evaluate_model.py). Install into a clean virtual environment:
#
#     pip install -r requirements.txt
#
# Tested on Python 3.12.

# --- API layer ---
fastapi==0.125.0
uvicorn==0.46.0
python-multipart==0.0.27

# Pydantic MUST stay on 1.x. main.py uses v1-style request models (class Config,
# .dict()); installing pydantic 2.x breaks the endpoints at import time.
pydantic==1.10.26

# --- model and data ---
# scikit-learn is pinned to the version the model artefact was serialised under.
# Loading nifty50_open_direction_logreg_v1.joblib under a different minor version
# raises InconsistentVersionWarning.
scikit-learn==1.6.1
pandas==2.1.4
numpy==1.26.4
joblib==1.3.2

# --- charts ---
matplotlib==3.7.5

# --- used by Predict_Future_N50_Opening.py to call the API over HTTP ---
requests==2.34.2
